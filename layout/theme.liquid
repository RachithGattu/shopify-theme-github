<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ shop.name }}</title>
    <link rel="stylesheet" href="{{ 'theme-clean.css' | asset_url }}">
  </head>
  <body>
    <main id="main">
      {{ content_for_layout }}
    </main>
  </body>
</html>